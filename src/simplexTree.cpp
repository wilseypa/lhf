#include <string>
#include <vector>
#include "simplexTree.hpp"

// simplexTree constructor
simplexTree::simplexTree(){
	simplexType="simplexTree";
}

double simplexTree::getSize(){
	size_t size = 0;
	
	//Calculate size of original data
	
	return size;
}


void simplexTree::insert(std::vector<double> vector){
	//insert into the simplex tree
	
	return;
}

void simplexTree::find(std::vector<double> vector){
	//find in the simplex tree
	
	return;
}

int simplexTree::vertexCount(){
	//Return the number of vertices in the tree
	
	return -1;
}

int simplexTree::simplexCount(){
	//Return the number of simplices in the tree
	
	return -1;
}
